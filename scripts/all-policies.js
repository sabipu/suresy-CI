jQuery(function() {
	

	var options = {
	  valueNames: [ 'id', 'name', 'email', 'status', 'created', 'type', 'monthly_premium', 'total_premium' ]
	};
	
	// Init list
	var contactList = new List('contacts', options);
});